# Python Reconciliation Demo

Same reconciliation pattern as the Alteryx demo, just in Python, using only the standard library so it runs anywhere.

```bash
python run_demo.py
python -m unittest discover -s tests -v
```

Input data is synthetic, made up for this repo.
